import PageLoadContainer from '../../ui/page-load-container/PageLoadContainer.component';
import { StyledAuthComplete } from './AuthComplete.styles';

function AuthComplete() {
  return (
    <StyledAuthComplete>
      <PageLoadContainer />
    </StyledAuthComplete>
  );
}

export default AuthComplete;
